# Normalize text line endings, but never touch binary files.
* text=auto

# Binaries — store byte-for-byte (no CRLF conversion)
*.pdf   binary
*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.icns  binary
*.exe   binary
*.dll   binary
*.woff  binary
*.woff2 binary
